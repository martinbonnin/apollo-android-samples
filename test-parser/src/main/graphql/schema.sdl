
type Query {
  randomAnimal: Animal
  randomPlant: Plant
}

interface Animal {
 name: String
}

interface Plant {
 name: String
}

type Cat implements Animal {
 name: String
 legs: Int
}

type Spider implements Animal {
 name: String
 legs: Int
}